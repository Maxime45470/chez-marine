<form method="post" action="{{ path('app_evenement_delete', {'id': evenement.id}) }}" onsubmit="return confirm('Vous allez supprimer un événement, êtes-vous sur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.id) }}">
    <button  class="btn btn-danger">Supprimer</button>
</form>
