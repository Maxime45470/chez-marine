<form method="post" action="{{ path('app_info_delete', {'id': info.id}) }}" onsubmit="return confirm('Vous allez supprimer une information, êtes-vous sur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
