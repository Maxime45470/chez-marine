{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('assets/css/navbar.css') }}">
{% endblock %}

<div class="test">
	<nav class="navbar navbar-expand-lg bg-light">
		<div class="container-fluid">
				<h2 class="navbar-brand">La semaine de mon enfant</h2>
			{% if app.user %}
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNav">
					<ul class="navbar-nav ms-auto">
						<li class="nav-item">						
								<a class="nav-link btn accueil m-1 p-1" href="{{ path('app_accueil') }}">Accueil</a>
						</li>
						<li class="nav-item">
								<a class="nav-link btn btn-primary m-1 p-1" href="{{ path('app_menu_hebdo') }}">Repas de la semaine</a>
						</li>
						<li class="nav-item">
								<a class="nav-link btn btn-primary m-1 p-1" href="{{ path('app_categories_index') }}">Photos</a>
						</li>
						<li class="nav-item">
								<a class="nav-link btn btn-info m-1 p-1" href="{{ path('app_mon_compte') }}">Mon compte</a>
						</li>
						<li class="nav-item">							
								<a class="nav-link btn btn-danger m-1 p-1" href="{{ path('app_logout') }}">Déconnexion</a>
						</li>
					</ul>
				{% endif %}
			</div>
		</div>
	</nav>
</div>
